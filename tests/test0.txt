echo "avant le if";
echo "abauzoids";
if test unmot = abc then
    echo "un truc je suppose";
    if test wow != dinguerie then
        echo "double if insane le truc"
    fi;
    if test quoi = feur then
        echo "plus d'idées oskour"
    fi
fi;
echo 'après le if';
echo "encore après";
exit
